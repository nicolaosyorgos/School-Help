.page-header
  %h1.os.uc All my notes

- unless @notes.blank?
  - @notes.each do |note|
    .note-wrapper  
      %a.note{ href: (url_for [note])} 
        .note-text
          %h2= note.title
          %p.note-date= time_ago_in_words(note.created_at) + " ago"

- else
  %h2 Add a Note
  %p It appears you haven't created any notes yet.... You better create one now, bitch
  %button= link_to "New Note", new_note_path